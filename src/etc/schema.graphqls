type Country {
    state_required: Boolean
    @resolver (class: "\\FriendsOfHyva\\ReactCheckout\\Model\\Resolver\\ExtensionAttribute")
    @doc(description: "Returns `true` for a country if 'State is Required for' is set in admin")
}
